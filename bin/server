const http = require('http');
const app = require('../gis');

const port = 8000;

app.set('port', port);
const server = http.createServer(app);

server.listen( port, () => { console.log( `Started server on ${ port }...` ); } );